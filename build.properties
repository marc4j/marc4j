lib.dir=lib
src.dir=src
test.dir=test
test.src.dir=test/src
build.dir=build
build.classes.dir=${build.dir}/classes
build.test-classes.dir=${build.dir}/test-classes
build.generated-src.dir=generated-src
dist.dir=dist
apidoc.dir=${build.dir}/apidoc
specversion=2.7
implversion=3
version=${specversion}.${implversion}
project.name=marc4j
jar.name=${project.name}-${version}.jar
jar.sources.name=${project.name}-${version}-sources.jar
jar.javadoc.name=${project.name}-${version}-javadoc.jar
test.jar.name=${project.name}_test-${version}.jar
java.compat.version = 1.6
ant.build.javac.source=${java.compat.version}
ant.build.javac.target=${java.compat.version}
